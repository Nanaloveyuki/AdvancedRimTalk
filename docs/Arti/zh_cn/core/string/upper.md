# upper

`upper(value)` 把文本中的字母转换为大写。

## 语法

```arti
let text = upper("hello")
let text2 = core.string.upper("hello")
let text3 = "hello".upper()
core.emit(text)
```

结果是 `HELLO`。`uppercase` 是同一函数的别名，见 [uppercase](uppercase.md)。

