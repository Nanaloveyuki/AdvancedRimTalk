# lower

`lower(value)` 把文本中的字母转换为小写。

## 语法

```arti
let text = lower("HELLO")
let text2 = core.string.lower("HELLO")
let text3 = "HELLO".lower()
core.emit(text)
```

结果是 `hello`。`lowercase` 是同一函数的别名，见 [lowercase](lowercase.md)。

